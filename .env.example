# ── Elevate CRM environment ──────────────────────────────────────────────
# Copy to `.env` (or export in your shell) before first run.

# Admin account. REQUIRED to use the app — there are NO hardcoded defaults.
# If these are unset, the server logs a setup message and login returns a
# clear "setup required" response instead of letting anyone in.
ADMIN_EMAIL=owner@elevate.studio
ADMIN_PASSWORD=change-me-to-a-strong-password

# Port the server binds. Use 3001 locally — port 3000 is the live agency
# site and must never be used by this app.
PORT=3001

# Optional: session signing secret. If unset, a random one is generated and
# persisted to data/.secret so sessions survive restarts. Set it explicitly
# in production (e.g. from your host's secret store).
# SESSION_SECRET=

# Set "true" when serving over HTTPS in production (sets the Secure cookie flag).
# COOKIE_SECURE=false

# Database configuration.
# When DATABASE_URL is set, Revzenta connects to PostgreSQL (production standard).
# If unset, Revzenta uses local SQLite in DATA_DIR (for offline development).
# DATABASE_URL=postgres://user:password@localhost:5432/revzenta

# Where the SQLite database file lives (used when DATABASE_URL is unset).
# Defaults to ./data (project-local).
# DATA_DIR=./data

# ── Emails (3g-4 intake/welcome + 3k password reset) ─────────────────────
# Resend API key. Emails are sent from Resend's built-in test sender
# ("Elevate Studio <onboarding@resend.dev>") — no domain needed until Phase 5.
# When unset, the server logs "[email] RESEND_API_KEY not configured — skipping
# <subject> to <to>" and the app works exactly as before (no emails, no crash).
# RESEND_API_KEY=
# While onboarding@resend.dev can only deliver to the account owner's verified
# address, set this to redirect intake/welcome/reset emails to the owner's
# mailbox for testing. The body is prefixed with "[TEST] Intended for <real
# recipient>". Default (unset): emails go to the real recipient's address.
# TEST_EMAIL_TO=higherpowerc@gmail.com
# Testing hook: override the Resend endpoint (the e2e suite points this at a
# mock). Defaults to https://api.resend.com/emails.
# RESEND_URL=https://api.resend.com/emails
