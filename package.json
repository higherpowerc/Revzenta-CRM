{
  "name": "revzenta-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Revzenta CRM — universal client pipeline management app.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && bun build ./index.html --outdir ./dist --minify",
    "check": "bun run typecheck && bun run build",
    "test:underwriting": "bun test/underwriting-engine.ts",
    "start": "bun ./server/index.ts",
    "start:local": "cross-env PORT=3001 bun ./server/index.ts",
    "dev": "cross-env PORT=3001 bun --watch ./server/index.ts",
    "seed": "bun ./server/seed.ts",
    "db:reset": "rm -f data/crm.db data/crm.db-shm data/crm.db-wal"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "cross-env": "^7.0.0",
    "typescript": "^7.0.2"
  }
}
